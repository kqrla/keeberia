# reference-image index

anne's reference photos backing the niche brainstorm (sept 26 2026) and
the keycap taxonomy / capsmith / clicketies docs. provenance: dropped
in this conversation, copied here so the repo is self-contained.

- **cat-paw-keycap-set.png**: the shape-axis reference. 3x3 block whose
  nine caps each have a sculpted paw pad, together reading as one cat
  paw with toe beans. cited by offerings/keycaps/README.md.
- **ref-cf4592002.png ... ref-9b35eb322.png (incl. ref-1fc11da9a.png)** (8 files): the
  positioning screenshots. subjects among them, per the conversation:
  a croissant topper with almond shavings on a tab-adjacent key, a
  burger topper on a macro key (capsmith examples), a chocolate-toned
  3x3 keycap block on a keychain clip and a single frog-face keycap on
  its own keychain clip (clicketies examples), a kuromi-shaped topper
  on an F1 key (licensed-character flag). the per-file subject mapping
  was not recorded when the images arrived and could not be recovered
  (vision keys down at relabel time), so these keep neutral names until
  relabeled. original upload ids preserved in the filenames.
