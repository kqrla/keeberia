# frontend

the frontend lives in its own repo: [kqrla/keeberia-front](https://github.com/kqrla/keeberia-front).

it is a lovable project — lovable syncs that repo directly, so a vendored copy
here would rot within a week. the engine repo owns the engines, the backend,
and the exported copy the site tells stories with (see /explain and
/project/positioning). if the frontend ever stops being lovable-hosted,
revisit this decision.
